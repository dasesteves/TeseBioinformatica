SELECT
  f.COD_F        AS CODIGO_FREQ,
  f.DESC_F       AS DESCRICAO_FREQ,
  f.XDIA         AS VEZES_DIA,
  CASE WHEN NVL(f.XDIA,0) > 0 THEN ROUND(24 / f.XDIA, 2) END AS INTERVALO_HORAS_EST,
  f.COD_SAPE,
  f.TABELA_SAPE,
  f.VALOR_SAPE
FROM PCE.PRF_FREQS f
ORDER BY f.COD_F;
